name: MoneyLoaner

services:
  localhost.sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: moneyloaner_sqlserver
    environment:
      SA_PASSWORD: "2LB%sL!fq%XNQ6&#cGG"
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"
    networks:
      - moneyloaner-net

  localhost.api:
    build:
      context: .
      dockerfile: MoneyLoaner.Api/Dockerfile
    container_name: moneyloaner_api
    ports:
      - "8080:80"
    depends_on:
      - localhost.sqlserver
    networks:
      - moneyloaner-net
    environment:
      - RanInDocker=true
      
  localhost.appserver:
    build:
      context: .
      dockerfile: MoneyLoaner.AppServer/Dockerfile
    container_name: moneyloaner_appserver
    ports:
      - "8081:80"
    depends_on:
      - localhost.api
    networks:
      - moneyloaner-net
    environment:
      - RanInDocker=true

networks:
  moneyloaner-net:
    driver: bridge